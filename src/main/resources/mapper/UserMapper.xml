<mapper namespace="online.stringtek.toy.framework.toymybatisdemo.mapper.UserMapper">
    <select id="selectList" resultType="online.stringtek.toy.framework.toymybatisdemo.pojo.User">
        select * from user
    </select>

    <select id="selectOne" parameterType="online.stringtek.toy.framework.toymybatisdemo.pojo.User" resultType="online.stringtek.toy.framework.toymybatisdemo.pojo.User">
        select * from user where id= #{id} and username= #{username}
    </select>
</mapper>